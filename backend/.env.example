# MongoDB connection string for development or production
MONGODB_URI=mongodb+srv://<username>:<password>@trustloopcluster.iwfu3pt.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT signing secret used for auth tokens
JWT_SECRET=your-secret-key

# Optional: port to run the backend server on
PORT=5000
